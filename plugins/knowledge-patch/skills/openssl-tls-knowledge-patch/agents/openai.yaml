interface:
  display_name: "Openssl Tls Knowledge Patch"
  short_description: "OpenSSL / TLS 4.0.0: ENGINE and legacy API removals, provider and FIPS changes, post-quantum TLS, server-side QUIC, ECH, kernel TLS, CLI/config updates, and..."
