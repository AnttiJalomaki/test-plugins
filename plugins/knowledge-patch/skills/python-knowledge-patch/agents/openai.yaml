interface:
  display_name: "Python Knowledge Patch"
  short_description: "Python 3.13–3.15 changes after the 3.12 baseline — free-threading, deferred annotations, t-strings, lazy imports, asyncio, standard-library migrations, and..."
